  public static boolean IsSymmetric(Node node) {
      
      int left = 0;
      int right = node.children.size() - 1;

      while(left <= right){
          leftChildren = node.children.get(left);
          rightChildren = node.children.get(right);

          if(leftChildren.size() != rightChildren.size()){
              
          }
      }
  }